services:
  - type: web
    name: abc-church-system
    env: node
    plan: free
    buildCommand: npm install && mkdir -p data
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production

